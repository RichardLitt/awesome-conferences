var req = require('request');
var values = function(obj) {
	for (var x in obj) {
		if (typeof obj[x] !== 'function') {
			console.log(x + '->' + obj[x]);
		}else{
			console.log(x + '-> function');
		}
	}
};

req.get('https://www.youtube.com/watch?v=oYmjOkvpqgo&list=UUISPcad-6svNxgViVr_syvA', function(err, resp, body) {
	resp.body = null;
	console.log(resp.request.href);
});